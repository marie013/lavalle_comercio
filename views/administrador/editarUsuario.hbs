<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Usuario</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 h-screen flex justify-center items-center">
    <section class="text-gray-600 body-font">
        <form class="w-full max-w-lg bg-white rounded-lg p-8 flex flex-col mt-10 md:mt-0 relative z-10 shadow-md"
            action="/usuario/editar/{{usuario.id_usuario}}" method="POST">
            <input type="hidden" name="_method" value="PUT">

            <h2 class="text-gray-900 text-lg mb-1 font-medium title-font">Editar Usuario</h2>
            <p class="leading-relaxed mb-5 text-gray-600">Modifica los detalles del usuario</p>

            <!-- Nombre del usuario -->
            <div class="relative mb-4">
                <label for="nombre" class="leading-7 text-sm text-gray-600">Nombre</label>
                <input type="text" id="nombre" name="nombre" value="{{usuario.nombre}}"
                    class="w-full bg-white rounded border border-gray-300 focus:border-indigo-500 focus:ring-2 focus:ring-indigo-200 text-base outline-none text-gray-700 py-1 px-3 leading-8 transition-colors duration-200 ease-in-out"
                    required>
            </div>

            <!-- Email del usuario -->
            <div class="relative mb-4">
                <label for="email" class="leading-7 text-sm text-gray-600">Email</label>
                <input type="email" id="email" name="email" value="{{usuario.email}}"
                    class="w-full bg-white rounded border border-gray-300 focus:border-indigo-500 focus:ring-2 focus:ring-indigo-200 text-base outline-none text-gray-700 py-1 px-3 leading-8 transition-colors duration-200 ease-in-out"
                    required>
            </div>
            <!-- Teléfono del usuario -->
            <div class="relative mb-4">
                <label for="telefono" class="leading-7 text-sm text-gray-600">Teléfono</label>
                <input type="number" id="telefono" name="telefono" value="{{usuario.telefono}}"
                    class="w-full bg-white rounded border border-gray-300 focus:border-indigo-500 focus:ring-2 focus:ring-indigo-200 text-base outline-none text-gray-700 py-1 px-3 leading-8 transition-colors duration-200 ease-in-out"
                    required>
            </div>
            <!-- Rol del usuario -->
            <div class="relative mb-4">
                <label for="rol" class="leading-7 text-sm text-gray-600">Rol</label>
                <select id="rol" name="rol"
                    class="w-full bg-white rounded border border-gray-300 focus:border-indigo-500 focus:ring-2 focus:ring-indigo-200 text-base outline-none text-gray-700 py-1 px-3 leading-8 transition-colors duration-200 ease-in-out">
                    <option value="true" {{#if usuario.rol}}selected{{/if}}>Administrador</option>
                    <option value="false" {{#unless usuario.rol}}selected{{/unless}}>Usuario</option>
                </select>
            </div>
            <!-- Botones -->
            <div class="relative flex space-x-4">
                <button type="submit"
                    class="flex-1 text-amber-500 bg-zinc-800 border-0 py-2 px-6 focus:outline-none hover:text-zinc-800 hover:bg-amber-500 rounded text-lg">Guardar</button>
                <button type="button"
                    class="flex-1 text-black bg-amber-500 border-0 py-2 px-6 focus:outline-none hover:bg-zinc-800 hover:text-amber-500 rounded text-lg"
                    onclick="window.location.href='/usuarios'">Cancelar</button>
            </div>
        </form>
    </section>
</body>

</html>